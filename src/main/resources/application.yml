spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/data_jpa
    username: fcc-spring-boot-demo-user
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
